{
  "name": "@vitamin/build",
  "scripts": {
    "build:mini": "gulp --require @esbuild-kit/cjs-loader",
    "build": "pnpm run clean:dist && vite build",
    "clean": "pnpm run clean:dist && pnpm run -r --parallel clean",
    "clean:dist": "rimraf dist"
  },
  "devDependencies": {
    "chalk": "^5.1.2",
    "consola": "^2.15.3",
    "fs-extra": "^10.1.0",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^8.0.0",
    "gulp-clean-css": "^4.3.0",
    "gulp-sass": "^5.1.0",
    "gulp-uglify": "^3.0.2",
    "vite": "^3.2.2",
    "vite-plugin-vue2": "^2.0.2"
  }
}
